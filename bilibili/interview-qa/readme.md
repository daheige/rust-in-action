# rust interview

interview 需要掌握的知识点

# Rust 中的可变引用是什么？

在 Rust 编程语言中，可变引用（也称为“可变借用”）是一种特殊的指针类型，它允许我们在不转移所有权的情况下对某个值进行修改。这与不可变引用形成对比，不可变引用只能用来读取数据，不能改变它们。
Rust 的安全性和内存管理模型围绕着所有权和生命周期的概念构建。这些概念确保了在任何给定的时间点，对于一个特定的值，最多只有一个可变引用存在。这种限制有助于防止数据竞争和其他并发问题，因为多个线程或作用域不能同时修改同一块数据。

Rust 中的引用本质上是一个指针，它引用该值但不拥有该值。引用允许父函数保留原始变量范围，同时允许子函数使用它。这意味着程序的多个部分可以访问相同的数据，而无需拥有它或制作副本。

Rust 中有两种类型的引用：不可变引用和可变引用。

- 不可变引用（Immutable
  references）：这些是只读引用，允许您借用值的不可变视图。当您对某个值有不可变引用时，您无法通过该引用来改变该值。不可变引用是使用 &
  符号后跟要借用的值创建的。
- 可变引用（Mutable references）：这些引用允许您借用值的可变视图。当您有一个值的可变引用时，您可以通过该引用来改变该值。可变引用是使用
  &mut 关键字创建的，后跟要借用的值。

当创建一个可变引用时，编译器会强制执行以下规则：

- 可变引用必须始终是唯一的：在同一作用域内，不能有其他可变引用或不可变引用指向同一个值。
- 不可变引用可以并存：如果有不可变引用指向一个值，那么可以有任意数量的不可变引用，但不能有任何可变引用。
- 可变引用不会自动提升为所有权：这意味着当你有一个可变引用时，你不能通过将其赋值给另一个变量来转移所有权。这是因为可变引用不是
  Copy 类型，因此不能通过简单的赋值操作复制。

# Rust 中的闭包

在 Rust 中，闭包（closures）是一种可以捕获其创建环境中的变量的匿名函数。它们允许你定义一个临时的一次性函数，可以在任何地方使用，并且能够访问外部作用域内的数据。
闭包有三种捕获机制，它们对应于三个不同的 Fn 属性：Fn, FnMut, 和 FnOnce。

在 Rust 中，闭包是一个类似于用于从封闭范围捕获变量的构造的函数，并且它可以作为值传递。当闭包从其封闭范围捕获变量时，Rust
会生成一个闭包对象，其中包含捕获的变量和闭包代码。
然后闭包对象可以用作普通值，并且可以称为常规函数。 Rust 闭包还可以推断其参数和返回值的类型，这使得它们非常灵活且易于使用。

Rust 为闭包提供了独特的所有权模型，使变量能够从封闭环境中捕获。当一个变量被闭包捕获时，它就获得了该变量的所有权，使其能够移动或修改该变量。

Rust 的不同类型的闭包是 Fn、FnMut 和 FnOnce。闭包的所有权模型根据您正在处理的闭包类型而有所不同。 Rust
的所有权模型确保闭包只能安全且可预测地访问捕获的变量，从而防止常见错误，例如释放后使用和数据竞争。

- Fn 是一种不会修改捕获值的闭包类型。它接收对捕获值的不可变引用，因此不允许修改这些值。如果闭包不修改任何捕获的变量，那么它可以实现
  Fn trait。
- FnMut 是一种可能修改捕获值的闭包类型。它接收对捕获值的可变引用，因此允许修改这些值。如果闭包修改了任何捕获的变量，那么它可以实现
  FnMut trait。
- FnOnce
  是一种只能被调用一次的闭包类型。它通过移动捕获的值来获取所有权，而不是借用。这意味着闭包一旦被执行，它的捕获值就不再可用，因为所有权已经转移给了闭包。当闭包返回时，所有被捕获的值都会被清理。如果闭包需要获取捕获值的所有权，那么它可以实现
  FnOnce trait。

注意，即使闭包实现了 FnOnce，也并不意味着它不能同时实现 Fn 或 FnMut。Rust 编译器会自动选择最合适的闭包类型。例如，如果你有一个只读的闭包，它仍然可以作为
FnMut 使用，因为它不需要修改捕获的值。
闭包的捕获机制是由编译器根据闭包体内部使用的变量决定的。这使得 Rust 能够在编译时确保正确地管理内存和所有权规则，避免悬垂指针和其他潜在的问题。

# 生命周期

在 Rust 中，生命周期是一个描述内存中数据引用和数据生命周期之间关系的构造。 Rust 编译器使用生命周期来理解和跟踪引用有效性的长度。
它就像附加在引用上的标签，指示引用的有效时间，因此可用于访问它所引用的数据。

# Rust 异步等待与调度（tokio为例）

在 Rust 中，异步编程是通过 Futures、async/await 语法以及运行时（如 Tokio）来实现的。这使得开发者能够编写非阻塞代码，从而提高系统资源利用率和并发性能。

## 异步等待

- 异步函数：Rust 中使用 async 关键字定义一个异步函数。这些函数不会立即执行其所有操作，而是返回一个表示未来完成值的 Future
  对象。
- await 关键字：await 关键字用于暂停异步函数的执行，直到给定的 Future 对象完成。这样就可以处理异步操作的结果而不阻塞其他任务。

## 调度

- Futures：Futures 是 Rust 中异步编程的核心概念。它们代表了尚未完成的计算，一旦计算完成，Future 就会包含其结果。Futures
  可以在不同的线程或任务中被调度，而不会阻塞其他计算。
- 异步执行程序：为了使 Futures 运行起来，需要一个异步执行程序。执行程序负责调度 Futures，并跟踪哪些 Future 已经准备好可以运行。Tokio
  和其他库提供了这样的执行程序，确保高效的多任务调度。
- 工作窃取算法：一些异步运行时（如 Tokio）采用了工作窃取算法进行任务调度。这种算法允许空闲的工作线程从繁忙的工作线程那里“偷取”任务，从而更有效地利用
  CPU 资源。

在 Rust 中，异步等待机制允许我们写出非阻塞的代码，而调度则是通过 Futures 和异步执行程序来管理这些非阻塞任务，确保系统的高效运行。

# Rust 中的自定义任务和 future 类型

在 Rust 中，自定义任务和 future 类型是异步编程的关键概念。它们允许开发者编写非阻塞代码，提高系统的并发性能和资源利用率。

## 自定义任务

Rust 并没有直接提供一个“任务”（Task）的概念，但可以通过 Futures 和 async/await
语法来实现类似于任务的功能。你可以创建一个异步函数，它代表了一个需要执行的任务，并且可以被调度到异步运行时（如 Tokio 或
Async-std）中去执行。

```rust
// my_async_task 函数就是一个自定义的异步任务。当这个函数被调用时，
// 它会返回一个 Future 对象，然后这个对象可以被提交给异步运行时进行调度和执行
async fn my_async_task() {
    // 异步操作...
}
```

## Future 类型

Future trait：Future 是 Rust 中异步编程的核心概念。它是一个 Trait，表示值将在未来某个时间点可用。Future 的定义如下：

```rust
pub trait Future {
    type Output;
    fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>;
}
```

其中 Output 是 Future 完成后返回的结果类型；poll 方法用于检查 Future 是否已经完成，如果尚未完成，则将当前任务注册到给定的上下文中以等待结果。

## async/await

async/await：为了更方便地使用 Future，Rust 提供了 async/await 语法糖(rust 1.39版本引入）。通过使用 async
关键字定义一个函数，该函数将自动返回一个
Future，而 await 关键字则可以在异步函数内部暂停执行，直到给定的 Future 完成。

| 版本            | 时间              | 内容                                                       |
|---------------|-----------------|----------------------------------------------------------|
| Rust 1.36     | 2019 年 7 月      | `std::future::Future` trait 进入标准库（此前在 `futures` crate 中） |
| **Rust 1.39** | **2019 年 11 月** | `async fn`、`async {}` 块、`.await` 语法稳定                    |
| Rust 1.75     | 2023 年 12 月     | 支持在 trait 中声明 `async fn`（`async fn in trait`）            |

设计背后权衡：

- 语法设计的波折：.await 前面有个点，是故意为之。Rust 曾经考虑过 await expr 和 expr.await() 两种写法，最终选择了
  expr.await（无括号、带点号），主要理由是：带点号可以明确表达"这是对值的一个后缀操作"，同时避免与函数调用语法混淆，还能在宏中更好地处理。
- 稳定之前：在 1.39 之前要用 nightly 编译器加 #![feature(async_await)]，或者直接使用 futures crate 0.1 时代的 and_then
  链式组合子写法。
- 语法糖的实质：async fn 会被编译器改写为返回一个实现了 Future 的匿名状态机结构体；函数体里的每个 .await 对应状态机的一个挂起点。这也是手写
  Future 状态机代码和 async 代码性能基本一致的原因——语法糖只是帮你生成了状态机。

```rust
// 你写的
async fn fetch() -> String {
    let data = request().await;
    data
}

// 大致等价于编译器生成的（简化版）
fn fetch() -> impl Future<Output = String> {
    async move {
        let data = request().await;
        data
    }
}
```

项目里用的 tokio::select!、tokio::time::timeout 等，底层都建立在 1.39 引入的这套语法之上。

在 Rust 中，自定义任务通常是通过异步函数来实现的，这些函数返回 Future 对象，从而能够被异步运行时进行调度和执行。而 Future
类型则是表示在未来某个时刻才能获得的结果，它是 Rust 中异步编程的基础构建块。

# Rust 中的 send 和 sync 特征

在 Rust 中，Send 和 Sync 是两种非常重要的 traits（特征），它们用于确保线程安全和数据的跨线程传递。

## Send 特征

Send trait 表示一个类型的数据可以在多个线程之间发送。也就是说，如果一个类型实现了 Send
trait，那么它的实例就可以被安全地从一个线程转移到另一个线程。Rust 编译器会自动为大多数基本类型实现这个 trait。

以下是一个实现了 Send trait 的自定义结构体：

```rust
struct MyStruct {
    data: u32,
}

unsafe impl Send for MyStruct {}
```

在这个例子中，我们手动为 MyStruct 实现了 Send trait，这意味着我们可以将 MyStruct 的实例在线程间传递而不会出现问题。

## Sync 特征

Sync trait 表示一个类型的引用可以在多个线程之间共享。也就是说，如果一个类型实现了 Sync
trait，那么它的引用可以被安全地从一个线程读取或修改而在另一个线程使用。Rust 编译器也会自动为大多数基本类型实现这个 trait。

```rust
use std::sync::Mutex;

struct MyStruct {
    data: Mutex<u32>,
}

impl Sync for MyStruct {}
```

在这个例子中，我们手动为 MyStruct 实现了 Sync trait，这意味着我们可以创建 &MyStruct 类型的引用并在多个线程之间共享。

- 请注意，对于包含可变状态的复杂类型，通常需要额外的同步机制（如锁）来保证线程安全性，并且只有当这些同步机制存在时，才能实现
  Sync trait。
- 在 Rust 中，Send 和 Sync traits 用于表示类型是否可以在多个线程之间安全地发送和共享。这对于编写多线程程序至关重要，因为不正确的数据传输可能会导致数据竞争、死锁等问题。

# Rust 中的 Mutex 和 Arc

在 Rust 中，Mutex 和 Arc 是用于线程安全编程的两种重要类型。它们分别解决共享数据的并发访问和数据的所有权问题。

## Mutex

Mutex（互斥锁）是一种同步原语，用于控制对共享资源的并发访问。当多个线程尝试同时修改同一块数据时，可能会导致数据竞争或不一致的状态。使用
Mutex 可以确保任何时候只有一个线程可以持有并修改数据。

互斥体是一种互斥原语，对于保护共享数据非常有帮助。它通过阻止等待锁可用的线程来跨多个执行线程安全地访问共享数据。
当使用互斥锁来保护资源时，在任何给定时间只有一个线程可以持有锁，从而防止数据争用并确保以安全且受控的方式访问资源。

Rust 标准库提供了 std::sync::Mutex<T> 类型，它封装了一个值 T 并提供了方法来锁定和解锁该值。当你试图获取 Mutex
中的数据时，如果没有其他线程正在使用它，你将获得一个可变引用；如果已经有其他线程正在使用，则当前线程将阻塞，直到该数据可用。

```rust
// 创建一个包含整数的 Mutex
let mutex = Mutex::new(0);

// 获取 Mutex 的可变引用，并增加其内部值
{
let mut num = mutex.lock().unwrap();
* num += 1;
}

// 通过 Mutex 再次读取并打印内部值
{
let num = mutex.lock().unwrap();
println ! ("The value is: {}", num);
}
```

## Arc

Arc（原子引用计数）是一个智能指针，用于在多个线程之间安全地共享所有权。通常，Rust 的所有权规则禁止将数据的所有权从一个线程转移到另一个线程。然而，使用
Arc，你可以创建一个值的引用计数副本，并在多个线程中传递这些副本，而无需转移所有权。

在 Rust 中，“原子”是指提供原子操作的类型，这意味着这些操作保证是不可分割的，因此在多个线程并发访问时不易受到竞争条件或数据损坏的影响。
Rust 提供了多种原子类型，包括 AtomicBool、AtomicIsize、AtomicUsize、AtomicPtr 等。这些类型允许您以线程安全且高效的方式对其底层数据执行原子读-修改-写操作

Arc 使用原子操作来管理引用计数，因此它是线程安全的。一旦所有 Arc 副本都被销毁，那么底层的数据也会被释放。

```rust
// 创建一个包含整数的 Arc 和 Mutex
let data = Arc::new(Mutex::new(0));

// 创建两个 Arc 的克隆并将它们传递给新线程
// 创建了两个 Arc 的克隆并将它们传递给两个不同的线程。
// 每个线程都可以安全地锁定和解锁 Mutex，从而修改共享数据
let thread_data1 = Arc::clone( & data);
let thread_data2 = Arc::clone( & data);

let t1 = thread::spawn(move | | {
// 获取 Mutex 的可变引用并在第一个线程中增加其内部值
let mut num = thread_data1.lock().unwrap();
* num += 1;
});

let t2 = thread::spawn(move | | {
// 获取 Mutex 的可变引用并在第二个线程中增加其内部值
let mut num = thread_data2.lock().unwrap();
* num += 1;
});

t1.join().unwrap();
t2.join().unwrap();

// 通过 Mutex 再次读取并打印内部值
{
let num = data.lock().unwrap();
println ! ("The final value is: {}", num);
}
```

在 Rust 中，Mutex 和 Arc 都是实现线程安全的重要工具。Mutex 用于控制对共享数据的并发访问，确保任何时候只有一个线程可以修改数据；而
Arc 则允许在多个线程之间安全地共享数据的所有权，而不需要实际转移所有权。

# Rust 中的共享数据和资源保护

在 Rust 中，共享数据和资源保护是通过所有权系统、借用规则以及一些特定类型（如 Mutex 和 Arc) 来实现的。这些机制确保了在并发编程中数据的一致性和安全性。

## 所有权系统

Rust 的所有权系统是一种内存管理机制，用于跟踪谁拥有一个值并负责其生命周期。每个值都有一个唯一的所有者，并且当所有者超出作用域时，该值将被自动释放。这种机制有助于防止数据竞争和悬挂指针等问题。

## 借用规则

除了所有权系统外，Rust 还使用借用规则来进一步控制对数据的访问。有两种类型的引用：可变引用（&mut
T）和不可变引用（&T）。在同一时间，一个值只能有一个可变引用或多个不可变引用。这保证了以下几点：

- 当存在一个可变引用时，没有其他线程可以读取或修改数据。
- 当存在多个不可变引用时，任何线程都不能修改数据，但可以同时读取数据。

## Mutex 和 Arc

对于需要在线程间共享和同步的数据，Rust 提供了 std::sync::Mutex<T> 类型。Mutex 是一种互斥锁，它封装了一个值 T
并提供了方法来锁定和解锁该值。当你试图获取 Mutex 中的数据时，如果没有其他线程正在使用它，你将获得一个可变引用；如果已经有其他线程正在使用，则当前线程将阻塞，直到该数据可用。

然而，通常我们不能直接将 Mutex 传递给另一个线程，因为这会违反 Rust 的所有权规则。为了解决这个问题，我们可以使用 std::sync::
Arc<T> 类型。Arc（原子引用计数）是一个智能指针，用于在多个线程之间安全地共享所有权。你可以创建一个值的引用计数副本，并在多个线程中传递这些副本，而无需转移所有权。

在 Rust 中，共享数据和资源保护是通过所有权系统、借用规则以及 Mutex 和 Arc 等类型来实现的。这些机制确保了在并发编程中的数据一致性、安全性和高效性。

# Rust 中的死锁和竞争条件

在 Rust 中，死锁和竞争条件是并发编程中常见的问题。虽然 Rust 的所有权系统和借用规则有助于防止许多与内存相关的错误，但它们不能完全避免这些问题。理解死锁和竞争条件的概念以及如何在
Rust 中处理它们对于编写安全、高效的并发代码至关重要。

## 死锁

死锁 是一种状态，其中两个或多个线程等待彼此释放资源，导致它们都无法继续执行。这种情况通常是由于同步原语（如互斥锁）的不当使用造成的。

考虑以下场景：

- 线程 A 持有锁 L1，并尝试获取锁 L2。
- 线程 B 持有锁 L2，并尝试获取锁 L1。

在这种情况下，线程 A 和 B 都会阻塞，因为它们都在等待对方释放所需的锁。这将导致程序无法继续执行，除非手动干预或者超时发生。
为了避免死锁，可以遵循一些原则：

- 避免嵌套锁定：不要在一个已经持有锁的代码块内尝试获取另一个锁。
- 锁定顺序：确保所有线程都按照相同的顺序获取锁，以避免循环等待。

## 竞争条件

竞争条件 是一种并发编程中的错误，它发生在两个或更多个线程访问和修改共享数据时，结果取决于线程的调度顺序。这种错误可能会导致不可预测的行为、数据不一致或逻辑错误。

Rust 中的竞争条件可以定义为多个线程（通常超过 2 个）尝试同时并发访问同一数据或内存位置，其中至少一次访问是写操作。这可能会导致未定义的行为，例如数据损坏、程序崩溃或安全​​漏洞。

```rust
fn data_race() {
    let counter = Arc::new(Mutex::new(0));
    let thread_data = counter.clone();

    let value = counter.clone();
    let t1 = thread::spawn(move || increment_counter(&value));
    // let t1 = thread::spawn(move || increment_counter(&thread_data)); // 无法编译
    let t2 = thread::spawn(move || increment_counter(&thread_data));

    t1.join().unwrap();
    t2.join().unwrap();

    println!("The final value is: {}", *counter.lock().unwrap());
}
```

为了防止竞争条件，可以使用各种同步机制，如互斥锁、信号量、条件变量等。在上面的例子中，我们已经使用了 Mutex
来保护对计数器的访问，但是我们在单独的线程中调用了 increment_counter 函数，而不是在原子操作中更新计数器。为了解决这个问题，我们可以使用原子类型（如
AtomicUsize 或 AtomicIsize）来实现无锁的递增操作。

在 Rust 中，死锁和竞争条件是并发编程中需要关注的问题。通过遵循正确的同步原则、使用适当的同步原语以及理解 Rust
的所有权和借用规则，可以有效地预防这些错误并编写安全、高效的并发代码。

# Rust如何保证内存安全

Rust 通过两种主要方法确保内存安全：

- 严格类型系统：Rust 的类型系统通过确保在编译时检查类型来帮助防止内存安全问题。这意味着编译器可以在代码运行之前捕获许多错误，例如尝试访问已移动或借用的值。
- 所有权和借用系统：在 Rust 中，每个值都有一个所有者，负责管理分配给该值的内存。 Rust
  确保某个值一次只有一个所有者，从而防止出现悬空指针或释放后使用错误等问题。除了所有权之外，Rust
  还采用借用方式。借用允许函数或方法临时借用程序另一部分拥有的值。

# Rust 中的 channel 和 select

在 Rust 中，channel 和 select 是异步编程中用于处理并发和通信的重要工具。它们分别提供了不同任务之间的数据传递和多路复用机制。

## Channel

- Channel 是在两个并发执行线程之间通信和传递消息的机制。channel 由发送方和接收方组成，从发送方到接收方的单向信息流。 Rust
  中的通道是使用 std::sync::mpsc 模块实现的。

- Channel 是一种线程安全的通信原语，它允许任务之间发送和接收消息。在 Rust 中，最常用的 Channel 实现是 Tokio 库中的 tokio::
  sync::mpsc 类型（多生产者、单消费者）和 tokio::sync::broadcast 类型（广播）。这些类型的 Channel 提供了异步接口，因此可以在非阻塞的任务中使用。

下面是一个使用 tokio::sync::mpsc 创建和使用 Channel 的简单示例：

```rust
async fn channel_task() {
    // 创建一个通道，其缓冲区大小为 10
    // 这里使用 tokio::sync::mpsc 多生产者/单消费者模式
    let (tx, mut rx) = mpsc::channel(10);

    // 在后台启动一个新的异步任务来监听通道
    tokio::spawn(async move {
        while let Some(msg) = rx.recv().await {
            println!("Received: {}", msg);
        }
    });

    // 发送一些消息到通道
    for i in 0..10 {
        tx.send(i).await.unwrap();
    }

    // 延迟一段时间以确保所有消息都被打印出来
    tokio::time::sleep(Duration::from_secs(1)).await;
}
```

在这个例子中，我们创建了一个可以存储最多 10 条消息的通道，并在一个新任务中监听该通道。然后，我们在主任务中发送一些消息，并等待一段时间以确保所有消息都被接收和打印。

## Select

Select 是一种用于实现异步 I/O 多路复用的技术。在 Rust 中，Tokio 库提供了一个名为 tokio::select!
的宏，它可以同时监控多个异步操作，并在其中任何一个准备好时执行相应的代码块。

```rust
async fn select_task() {
    let timeout = time::sleep(Duration::from_millis(200));
    let mut interval = tokio::time::interval(Duration::from_millis(100));

    tokio::select! {
        _ = timeout => {
            println!("The timeout occurred first!");
        },
        _ = interval.tick() => {
            println!("The interval ticked first!");
        },
    }
}
```

在这个例子中，我们创建了两个定时器：一个将在 200 毫秒后超时，另一个每 100 毫秒发出一次“滴答”事件。tokio::select!
宏会同时监控这两个异步操作，并在其中一个准备好时立即执行相应的代码块。

在 Rust 中，channel 和 select 分别提供了任务间通信和异步 I/O 多路复用的功能。通过结合使用这两种机制，开发者可以编写高效且复杂的异步应用程序。

# Rust 中声明全局变量

在 Rust 中，您可以使用 static 关键字声明全局变量。 static 关键字声明具有静态生命周期的全局变量，这意味着它在程序执行的整个持续时间内都存在。
声明全局变量需要指定类型，并且必须有常量表达式用于初始化。此外，由于可以从多个线程访问全局变量，因此在使用可变全局变量时必须确保处理同步。

```rust
// 生命全局变量static修饰
static mut APP_DEBUG: bool = true;

// AtomicBool 在 x86 上就是一条指令，性能上没有任何可感知差异。
// 除非你在写操作系统底层、FFI 边界这类必须和 static mut 打交道的代码，否则都应该选 AtomicBool。
static APP_DEBUG2: AtomicBool = AtomicBool::new(true);

// 使用方式
unsafe {
APP_DEBUG = false;
// addr_of! 产生 *const bool 裸指针，不创建引用
// println!("APP_DEBUG: {}", *std::ptr::addr_of!(APP_DEBUG));

// 读的时候用 read_volatile，语义上更明确，每次都真正去内存读，不优化缓存
println!(
    "APP_DEBUG: {}",
    std::ptr::read_volatile(std::ptr::addr_of!(APP_DEBUG))
);
}

APP_DEBUG2.store(false, Ordering::SeqCst);
println!("APP_DEBUG2: {}", APP_DEBUG2.load(Ordering::SeqCst));
```

| 方案                        | 代码量 | 线程安全   | 需要 unsafe |
|---------------------------|-----|--------|-----------|
| `static mut` + `addr_of!` | 多   | 无保障    | 是         |
| `AtomicBool`              | 少   | 由编译器保证 | 否         |

# Rust 中的 Option 和 Result

在 Rust 中，option和result都是表示出现错误或成功值的可能性的类型。然而，它们之间的一些区别是：

- Option 代表可能存在或不存在的计算值。例如，当函数在集合中查找项目时可能不返回值时，就可以使用它。该选项可以包含“Some（值）”或“none”，通常用于避免空指针错误。
- Result 表示操作结果，可以是成功，也可以是失败，如果失败则带有相关的错误值 (E)。
  “结果”类型通常用于函数可能由于多种原因而失败的情况，因此可以以结构化的方式处理错误情况。

# Rust 中的宏

Rust 支持两种宏：过程宏和声明宏。

- 过程宏在编译时通过语法树生成代码。程序宏在其包内定义，可以通过自定义属性调用。
- 声明性宏使您能够匹配 Rust 代码中的模式并使用这些模式生成新代码。声明性宏是使用 Macro_rules 定义的！宏，它采用一组匹配规则和一组替换模式。

总的来说，Rust 拥有强大的宏系统，可以灵活地以各种方式生成代码。然而，宏也可能很复杂且难以调试，因此应谨慎使用它们。

## 过程宏

在 Rust 中，过程宏是一种宏，允许您定义可在代码中使用的自定义语法扩展。过程宏被实现为 Rust 函数，该函数接受 Rust
代码作为输入，以某种方式对其进行操作，然后使用新的 Rust 代码生成输出。过程宏用于在编译时生成代码。

## Rust 中的声明性宏

在 Rust 中，声明性宏允许您定义一个与输入代码相匹配的模式，然后根据该模式生成新代码。声明性宏是使用 Macro_rules！ 定义的宏。
macro_rules! 宏将一组规则作为输入，这些规则定义要匹配的模式和要生成的代码，并生成实现宏的代码。

# Rust 中的 Cargo.toml 和 Cargo.lock 文件是什么

Cargo.toml 是 Rust 使用的名为 Cargo 的包管理器中使用的配置文件。该文件包含元数据并指定有关项目名称、版本、构建设置和依赖项的信息。
该文件以“TOML”格式编写，即 Tom’s Obvious Minimal Language，这是一种简单的配置语言。通过使用
Cargo.toml，您可以轻松管理项目的依赖项和构建设置，从而更轻松地与他人共享和协作。

Cargo.lock 包含与 Rust 项目的依赖项相关的信息，例如传递依赖项。该文件的目的是确保构建新项目的任何人都将使用与项目的最后版本相同的依赖项，以避免依赖项冲突并确保可重用的构建。

# Rust 中的枚举

在 Rust 中，枚举是一种类型，使开发人员能够定义一组命名值或数据。这些值可以有多种变体，并且还可以有附加或可选数据。
枚举在 Rust 中非常有用，用于表示可以采用一组有限值的数据，例如一周中的几天或用户界面的选项。他们还可以定义自定义错误类型或其他复杂的数据结构。

# Rust 中的条件编译

在 Rust 中，条件编译是一项功能，使开发人员能够有选择地使用预定义条件编译代码的特定部分。此功能通常用于开发特定于平台的代码或为特定构建配置创建功能。
条件编译是使用 #[cfg] 属性来实现的。此属性可以指定确定特定代码块是否应包含在最终编译的二进制文件中的条件。

# 什么是构建脚本 build.rs

构建脚本是 Rust 中的一个特殊源文件，该文件在项目的构建过程中执行。构建脚本执行多项任务，包括以下任务：

- 生成代码
- 设置环境变量
- 编译外部依赖
- 配置构建选项

# Rust 中的迭代器是什么

在 Rust 中，迭代器是一个提供一系列值的过程，可以使用“for 循环”等迭代器方法来迭代这些值。迭代器有助于实现循环。要在 Rust
中使用迭代器，通常会创建一个实现 Iterator 特征的类型实例，然后在循环中或与其他迭代器方法一起使用它。

# 函数指针怎么理解

函数指针是一种类型，表示指向函数的指针，其身份在编译时可能未知。它使开发人员能够存储对特定函数的引用，该函数可以在代码中再次调用。

当您必须将一个函数作为参数传递给另一个函数或将函数存储在数据结构中时，函数指针非常有用。在 Rust 中，您可以使用 fn
关键字和 *const 或 *mut 指针语法来定义函数指针。

# Rust 中的元组

元组是不同类型值的集合。它类似于数组，但与数组不同的是，元组可以包含不同类型的值。元组是使用括号构造的，其中的值用逗号分隔。

```rust
let my_tuple = (10, "hello", true);
```

# match 模式匹配

match 语句是一个控制流运算符，它提供了一种强大的机制，可以根据变量的匹配模式将控制转移到特定的代码块。它使您能够比较一系列模式中的值，然后根据模式匹配执行相关的代码块。
当执行 match 语句时，Rust 将按顺序尝试每个模式，并执行与第一个匹配该值的模式关联的代码。

# Rust 中的结构体和枚举有什么区别

虽然 struct 和 enum 都用于在 Rust 中指定自定义数据类型，但它们具有不同的属性和用途。结构体是一种数据结构，它将不同类型的相关数据组合到一个单元中。结构通常用于表示程序中的实体或对象。

枚举是一种用于表示一组命名值的数据类型。枚举通常用于定义给定值的一组有限的可能状态或选项。枚举中的每个命名值称为变体。

# Rust 中的错误处理

Rust 有多种机制来处理错误。让我们看一下几个选项。

- Result类型：Rust 提供了内置的Result类型，使函数能够返回值或错误。结果值用 Ok(value)或Err(error)表示。如果出现错误，该函数将提供有关错误的信息。
- Option类型：选项类型与结果类型类似，但用于值可能存在或不存在的情况。当值是可选的或函数可能无法返回值时，通常会使用选项类型。
- Panic! macro：如果程序面临致命错误，则恐慌！宏机制有助于停止程序的执行并提供相关的错误消息。当程序遇到严重错误时，这特别有用，可以帮助终止程序执行。
- Error Handling Libraries: ：Rust 也有几个错误处理库，例如标准库的 Error 特征和流行的 crates，如 thiserror、anyhow 和
  failure，它们为错误处理提供了更高级的功能，例如自定义错误类型、回溯和错误链接。

# 标准库在 Rust 中的作用是什么

Rust 中的标准库包含提供该语言核心功能的模块集合。标准库随 Rust 的每次安装一起打包，它提供了广泛的功能，例如 I/O
操作、数据类型、网络功能、并发协议等。
标准库的设计目标是高效、安全且易于使用。它还具有完整的文档记录，包含大量 API 文档和在线示例。

# Rust 中的异步编程

Rust 中的异步编程涉及编写可以执行非阻塞操作而不阻塞主线程的代码。这是使用 Rust 的 async/await 语法和 Rust 标准库的异步运行时来完成的。

在 Rust 中，异步编程是通过 future 完成的，代表一个可能不可用的值。可以使用 map、and_then 和 or_else 等组合器组合这些
future，以创建应异步执行的操作序列。
async 关键字用于定义返回 future 的函数，await 关键字用于暂停当前函数的执行，直到 future 完成。

# Rust 并发模型

Rust 提供了用于编写并行程序和实现并发的各种功能。 Rust 中的并发模型主要基于所有权和借用概念，确保内存安全并防止常见的并发错误，例如死锁和数据竞争。
Rust 中的每个值都由单个线程拥有，并且所有权可以通过消息传递跨线程转移。 Rust 的并发模型被设计为安全高效，为构建并发和并行程序提供了一组强大的工具。

# 如何在 Rust 中执行 I/O

Rust 标准库中的 std::io 模块用于执行输入/输出 I/O 操作。您可以通过 std::io 模块获得一组用于高效执行 I/O 操作的结构、函数和特征。

您还可以通过 std::io::stdout() 函数执行输出操作，该函数将处理标准输出，然后使用 write() 或 writeln!() 方法将数据写入输出流。

# Rust 中的文档

在 Rust 中，文档是编写代码的重要组成部分。 Rust 有一个名为 Rustdoc 的内置文档系统，允许开发人员使用注释记录其代码并生成可在
Web 浏览器中查看的 HTML 文档。
Rustdoc 使用一种称为“Rustdoc 注释”的语法来记录代码。 Rustdoc 注释以 /// 开头，并放置在文档项（例如函数、结构或模块）的正上方。

# Rust 中如何处理多线程

Rust 通过标准库提供了对多线程的内置支持。 Rust 以轻量级执行单元的形式提供线程，具有在程序中并发运行的能力。

std::thread::spawn 函数允许在 Rust 中创建一个新线程，它采用一个表示将在线程中运行的代码的闭包。 Rust
还提供了几个同步原语来帮助管理跨线程共享数据的访问，包括互斥体、信号量和通道。

# 如何使用 Rust 的标准集合（Vec、HashMap 等）

Rust 的标准集合，例如 Vec、HashMap 和 HashSet，通常在 Rust 程序中用于管理和操作数据集合。以下是如何使用这些集合的一些基本示例：

- Vec: A Vec ("vector") 是 Rust 的内置动态数组。要创建新向量，可以使用 Vec::new() 方法或简写符号（如 vec![1, 2, 3]
  ）来创建具有初始值的向量。
- HashMap: HashMap 是 Rust 的内置哈希表实现。它允许您存储键值对，其中每个键都是唯一的。
- HashSet: HashSet 与 HashMap 类似，但仅存储唯一键，没有任何关联值。

# Rust 中的trait系统是什么

trait系统涵盖了为特定类型定义的方法集合。特征系统支持通用编程和代码可重用性。特征还指定类型可以实现的一组属性、能力或行为。
特征可用于定义可供任何想要使用该特征的类型实现的方法、相关类型和常量。多个特征可以通过类型来实现，使它们能够集成各种功能和行为。

## Trait 对象与泛型

dyn Trait vs impl Trait 的区别，以及对象安全（object safety）的含义，是中高级岗必考点。

简单说：impl Trait 编译时静态分发，性能好；dyn Trait 运行时通过 vtable 动态分发，有一层间接调用开销。如果一个 Trait 包含返回
Self 的方法，它不是对象安全的，不能用 dyn Trait

# Rust 中的内存模型是什么

- Rust 中的内存模型旨在通过执行一组控制 Rust 代码与内存交互方式的规则来提供安全性和性能。这些规则由 Rust
  编译器强制执行，它在编译时执行多项检查，以确保 Rust 代码不会违反内存安全规则。
- Rust 中的内存模型基于所有权和借用。所有权是指 Rust 中的每个值都有一个所有者，并且一次只能有一个所有者。
- 借用是指程序的另一部分可以借用某个值的想法，这允许程序的该部分访问该值而不取得其所有权。借用对于如何使用借用值有严格的规则，这些规则由
  Rust 编译器强制执行。Rust 中的内存模型还包括生命周期的概念，它用于跟踪值的生命周期并确保借用的值不会比它们借用的值的寿命更长。

# Rust 中如何使用标准字符串类型

Rust 有两种主要的字符串类型：String 和 str。

- string 是可增长的、堆分配的字符串类型，而 str 是字符串切片，它是连续 UTF-8 字节序列的视图。您可以使用 String::new()
  函数创建一个新字符串。
- 要从string创建 (&str) 字符串切片，只需使用该string的引用即可。要操作字符串，您可以使用 String 和 str 类型提供的各种方法，例如
  len()、is_empty()、chars()、as_bytes()、split() 和 trim() 等。

# Rust 如何支持网络

Rust 的标准库“std”提供了用于网络的模块。 std::net 模块支持多种网络协议和机制，包括 IPV4、IPV6、TCP 和 UDP。

- TCP and UDP sockets：Rust 提供了低级原语，用于分别使用 std::net::TcpStream 和 std::net::UdpSocket 类型创建 TCP 和 UDP
  套接字并与之交互。
- TCP and UDP listeners：Rust 还提供了分别使用 std::net::TcpListener 和 std::net::UdpSocket 类型创建 TCP 和 UDP 监听器的原语。
- IPv4 and IPv6：Rust 支持 IPv4 和 IPv6 地址和套接字。
- HTTP：Rust 有几个用于处理 HTTP 的包，包括“hyper”和“reqwest”，这些包为构建 HTTP 客户端和服务器提供了高级抽象。

# 如何使用 Rust 进行 Web 开发

Rust 是 Web 开发中使用的最高效的编程语言之一，具有各种功能和对 Web 开发的全面支持。 Rust 为 Web 开发提供的一些主要功能如下：

- 异步编程：Rust 内置对异步编程的支持，使开发人员能够生成高效且非阻塞的代码来管理多个并发请求
- Web 框架：Rust 有许多可用的 Web 框架，包括 Rocket、Actix 和 Warp，它们为 Web 开发提供了坚实的基础。
- 安全性：Rust 为 Web 开发提供了强大的安全机制，它使用所有权和借用机制来确保安全的内存管理并防止内存泄漏和空指针异常等突出问题。
- 跨平台兼容性：Rust 提供跨平台兼容性，因为它可以跨各种平台编译，从而使其成为 Web 应用程序的理想选择。

# Rust 如何支持数据库编程

Rust 是一种流行的系统编程语言，通常用于构建高性能且可靠的应用程序。虽然 Rust 并不是专门为数据库编程而设计的，但它提供了多个库和工具，可以有效地使用数据库。

一些流行的数据库编程 Rust 库包括

- Diesel：Diesel 是一个流行的 Rust ORM（对象关系映射）库，它提供类型安全且可组合的查询构建器。它支持多种数据库，包括
  PostgreSQL、MySQL 和 SQLite。
- Postgres：Postgres 是一个用于处理 PostgreSQL 数据库的 Rust 库。它提供了安全且符合人体工程学的 API，可以轻松地与 Postgres
  进行交互。
- SQLx：SQLx 是一个 Rust 库，它提供了用于处理多个数据库（包括 PostgreSQL、MySQL 和 SQLite）的统一
  API。它支持同步和异步操作，并提供类型安全的查询生成器。

Rust 为数据库编程提供了一组强大的工具和库，使其成为构建与数据库交互的高性能且可靠的应用程序的绝佳选择。

# Rust 如何管理不安全代码

Rust 旨在提供低级系统编程的功能和性能，同时确保安全性并防止常见错误，例如空指针、数据争用和缓冲区溢出。然而，在某些情况下，开发人员需要直接使用低级内存，这可能会导致安全问题。
Rust 编译器提供了多种功能来帮助管理不安全代码：

- 原始指针：Rust 提供与 C 类似的原始指针，允许开发人员执行指针算术和转换等低级操作。
- Unsafe 的函数和方法：Rust 提供了一些被标记为不安全的函数和方法，这意味着它们需要开发人员手动确保它们的使用正确。
- Unsafe blocks：Rust 允许开发人员将代码块标记为不安全，从而允许他们执行否则不允许的低级操作。然而，Rust
  要求不安全块包含在安全函数或方法中，以便编译器可以确保程序的整体行为是安全的。

# Rust 如何支持泛型

Rust 通过使用类型参数来支持泛型。类型参数使开发人员能够定义函数或泛型类型，而无需为每种类型编写单独的代码。
要定义泛型类型或函数，首先使用尖括号 <> 声明一个或多个类型参数。通过使用泛型，Rust
提供了一种强大而灵活的机制，用于编写适用于不同类型的可重用代码，同时仍然保持类型安全性和性能。

# crates 托管

crate是打包在语言中的编译单元。它可以被视为一个模块或一个库，其中包含可以在各种 Rust
项目中重用和共享的代码。一个crate可以有多个包含函数、枚举、结构和其他属性的模块。
将代码组织成包和模块有助于以简化、模块化的方式实现代码的可重用性。 Rust crate 发布在 Rust 包注册表（称为 http://crates.io
）上。这是一个中央存储库，开发人员可以在其中发布他们的包，其他开发人员可以在他们的项目中搜索和使用它们。

# Rust 中的复制和克隆特征有什么区别

复制和克隆特征决定了 Rust 类型应该如何复制或克隆。 Copy 特征用于复制成本较低的类型，例如数字、指针和布尔值。当实现 Copy
特征的类型的值被分配给另一个变量时，会生成该值的按位副本，并且两个变量都可以独立使用。

Clone 特征用于复制成本较高或具有所有权语义的类型，例如字符串、向量和在堆上分配内存的其他类型。当实现 Clone
特征的类型的值被克隆时，将创建该值的新副本，并且原始值和克隆可以独立使用。

# module和crate之间的区别

在 Rust 中，模块是一种在文件内或跨多个文件组织代码的方式，而 crate 是 Rust 中生成二进制文件或库的编译单元。
模块是使用 mod 关键字定义的，可以包含 Rust 代码，例如函数、结构、枚举、常量和其他模块。一个模块可以嵌套在另一个模块中，形成模块的层次结构。这允许创建有组织且可重用的代码。

另一方面，板条箱是 Rust 源文件的集合，这些文件一起编译成一个单元。包可以是生成可执行程序的二进制包，也可以是生成可链接到其他程序的库的库包。

当您创建 Rust 项目时，您从一个板条箱开始，并且该板条箱内可以有多个模块。模块用于组织 crate 内的代码，使其更易于维护和重用。

# 静态生命周期的目的是什么

在 Rust 中，静态生命周期代表具有全局生命周期的数据，即程序执行的整个持续时间。其目的是确保数据在整个程序执行过程中保持有效，并且静态生命周期说明符定义了它。

当变量被声明为具有静态生命周期说明符时，会在整个程序生命周期内为其分配一个内存位置。静态变量可以定义为常量或可变变量，并且可以从程序中的任何位置访问。

# 特征对象和泛型类型之间的区别

在 Rust 中，特征对象和泛型类型是实现多态性的两种不同机制。
泛型类型是通过一个或多个其他类型参数化的类型。当使用泛型类型定义函数或结构体时，调用者可以指定调用函数或实例化结构体时使用的具体类型。这允许灵活且可重用的代码可以在不同类型上运行。

另一方面，特征对象是对实现特定特征的对象的类型擦除引用。 Trait 对象是通过使用“dyn”关键字来指定该对象实现的 Trait
来创建的。这允许动态分派，其中调用的实际方法是在运行时根据对象的具体类型确定的。

# 生命周期

生命周期参数是语言类型系统的一个功能，用于表达不同值及其生命周期之间的关系。生命周期表示值在内存中有效且可访问的持续时间。

Rust 中的每个值都有一个生命周期，Rust 的所有权和借用规则旨在确保值的生命周期得到正确管理。生命周期参数由撇号 (')
后跟名称表示，例如“a”。它们可以用在函数签名、结构定义以及其他涉及具有生命周期的值的地方。

# 迭代器和生成器有什么区别

在 Rust 中，迭代器是一个特征，它定义了可以使用 for 循环或其他迭代结构进行迭代的元素序列。

迭代器根据需要生成一系列值，并且可以迭代任何实现 Iterator 特征的集合。
另一方面，生成器是一种迭代器，它可以延迟地按需生成值，而不是预先急切地生成所有值。生成器是使用yield关键字定义的，可用于表示无限或非常大的序列。

# Rust 中的智能指针

智能指针是一种数据类型，与常规指针相比，它提供了附加功能。智能指针通过在不需要时自动释放内存来帮助管理内存。这有助于避免悬空指针和内存泄漏等问题。

智能指针是一种具有指针特性但具有附加功能的数据结构。 Rust 中的智能指针的一个示例是 Rc 类型，它提供值的共享所有权。

# Rust 中如何使用切片

切片是对内存块中元素序列的指针或引用。切片用于访问以连续序列存储在内存中的数据卷。
切片由类型 &[T] 表示，其中 T 是切片中元素的类型。切片可以从向量、数组、字符串和其他使用 std::slice::SliceIndex 特征的集合类型创建。

切片通常用于将集合的一部分而不是整个集合传递给函数。切片是轻量级且高效的，因为它们仅包含序列开头的指针和长度。

切片是 Rust 的一项强大功能，它允许您有效地访问和操作集合的一部分，而无需复制其数据。以下是 Rust 中切片的一些常见用例：

- 访问数组或向量的部分：您可以使用语法 [start..end] 创建一个指向数组或向量的一部分的切片，其中 start 是要包含的第一个元素的索引，end
  是要排除的第一个元素的索引。
- 将参数传递给函数：切片通常用于将集合子集传递给函数。
- 字符串操作：Rust 的字符串类型（String）是作为字节向量实现的，因此在操作字符串时广泛使用切片。
- 二进制数据操作：切片还用于处理二进制数据，例如读取或写入文件。 std::io 模块提供了许多将切片作为参数来读取或写入数据的函数。

# Rust异步tokio

- Future trait 的 Poll 机制：async 函数返回一个 Future，调用 .await 会 poll 它；返回 Pending 时任务挂起，等 wake 通知再次
  poll
- Tokio 运行时：多线程调度器 vs 单线程调度器，spawn vs spawn_local 的适用场景
- Pin/Unpin 为什么存在：async 函数编译成含自引用的状态机结构体，移动后引用失效——Pin 防止这个结构体被移动

Pin/Unpin 是 Rust 异步面试里最容易让候选人卡住的点。能用一段话讲清楚"为什么 Future 需要 Pin"的候选人，基本属于技术面里的加分项。

# Rust safety

- 安全性是Rust的显著特征之一，其安全特性使其与其他语言形成最显著差异。得益于名为借用检查器的功能，Rust能够提供强大的安全保障。
- 在C和C++等语言中，内存管理是半手动过程，开发者在考虑内存管理时必须了解实现细节。而Java、Go和Python等语言采用自动内存管理（即垃圾回收机制），以牺牲部分性能为代价，掩盖了内存分配与管理的细节。
- Rust的借用检查器通过在编译时验证引用来运作，而非运行时进行引用计数或垃圾回收。这一独特特性在编写软件时也会带来挑战，特别是对初次接触借用检查器的开发者而言。
-

借用检查器作为Rust编译器rustc的组成部分，会确保任何给定对象或变量在同一时间内最多只能存在一个可变引用。虽然允许对对象或变量存在多个不可变引用（即只读引用），但任何时候都只能存在一个活跃的可变引用。Rust通过检查可变引用与不可变引用永远不会重叠，从而保证内存安全。

- Rust采用资源获取即初始化（RAII）机制来追踪变量及其所有引用的作用域范围。一旦超出作用域，内存即可被释放。借用检查器会阻止对已超出作用域变量的引用，并且只允许存在一个可变引用或多个不可变引用，但绝不允许两者同时存在。
-

同时，借用检查器同样为并发编程提供了安全保障。当共享数据时（例如在不同线程之间），可能会出现竞态条件。在大多数情况下，根本原因都是相同的：同时存在多个可变共享引用。然而，Rust通过"
唯一可变引用&mut T"或"一个及以上不可变引用&T"的规则，从根本上杜绝了多个可变引用并存的可能性，从而确保避免数据同步问题，或至少不会无意中引发这类问题。

-

Rust的借用检查器初学时较难掌握，但很快你就会发现这是Rust最出色的特性。与Haskell等语言类似，只要代码能通过编译（再配合充分测试），通常就能保证代码正常运行且永不崩溃。虽然有时候存在例外情况，但总体而言，用Rust编写的代码不会因常见内存错误（如缓冲区越界读取、内存分配与释放处理不当）而崩溃。
